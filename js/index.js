const mainTitle = document.getElementById('main-title');

mainTitle.addEventListener('click', () => {
    if (mainTitle.style.color = 'red'); {
        mainTitle.style.color = '#253a62';
    } else if (mainTitle.style.color = '#253a62') {
        mainTitle.style.color = 'red';
    }

})